/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,  // Strict mode se development me madad milti hai

  // basePath: '',        // Yeh comment me chhoda hai, sirf tab use karna jab app subfolder me host ho
  // trailingSlash: false, // URLs ke end me slash chahiye to true karo, nahi to false hi rahe

  // Future me agar koi aur setting chahiye to yahan add kar sakte ho
};

export default nextConfig;
